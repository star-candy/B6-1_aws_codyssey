#### EC2 접속을 위한 공개, 개인 키 조합
- 공개 키는 EC2, 개인 키는 사용자에게 저장
- 사용자는 개인 키를 통해 특정 EC2 인스턴스에 접속 가능
- 사용자 개인 키는 특정 파일 형태로 다운로드 될 것(pem)

1. AWS cloudShell 상용 시 shell에 파일 등록하여 인스턴스 접속 가능
2. Connect 방식의 경우 파일 직접 등록 불가 : 
	-> shell에서 내부 key 값 복사 후 vi 명령으로 직접 파일 등록하기