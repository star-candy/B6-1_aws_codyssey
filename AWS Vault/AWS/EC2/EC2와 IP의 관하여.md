#### 1. public IP
- 전체 인터넷에서 각 기기당 하나씩 부여된 IP
- 전체 인터넷 영역에서 통신 가능
- EC2 SSH 통신 시 public IP 사용
- EC2 종료, 재실행 시 변경됨 유의
#### 2. private IP

- 특정 사설 망 내부에서 사용되는 IP
- 사설망 내에서는 유일하나 타 사설망 간에는 중복 가능
- SSH 통신시 사용 불가 (AWS, SSH는 타 사설망)
- 
#### 3. Elastic IP
- AWS에서 최대 5개 할당 가능
- EC2 종료, 재실행 할 경우에도 유지됨